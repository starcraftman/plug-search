Execute (Dump Test DBs):
  Log string(keys(g:psr_plugs))
  Log string(keys(g:psr_tags))

Execute (Open Window):
  PSearch zzz
  AssertEqual getline(1), "Plug Search"
  AssertEqual getline(2), "-----------"
  quit!

Execute (Search Tags):
  PTags make
  Assert stridx(getline(4), "rdnetto/YCM-Generator") != -1
  quit!

Execute (Search Term):
  PSearch You
  Assert stridx(getline(4), "rdnetto/YCM-Generator") != -1
  Assert stridx(getline(5), "Valloric/YouCompleteMe") != -1
  quit!

Execute (Open Info Window):
  PSearch kien
  normal! 4gg
  normal o

Expect (Open Info Window):
  kien/ctrlp.vim
  --------------
  Fuzzy file, buffer, mru... finder.

  PLUGIN UNMAINTAINED
  Active Fork: ctrlpvim/ctrlp.vim
  Alternatives:
  * ctrlpvim/ctrlp.vim
  Tags:
  - buffers
  - inactive
  - indexing
  - mru
  - tags
